# OBX Lot Solutions — old Squarespace → new-site 301s (Cloudflare Pages).
# STAGED. No DNS cutover without Ray's go. Confirm left-side OLD paths against the Google Ads
# "Landing pages" Final-URL export before cutover (see websites/301-REDIRECT-MAP.md).
#
# IMPORTANT: targets are EXTENSIONLESS. Cloudflare Pages serves foo.html at /foo and canonicalizes
# /foo.html -> /foo. So NEVER add a rule whose SOURCE is a real new page (e.g. /services, /contact,
# /about) pointing at /x.html — that loops against the .html->clean-URL redirect (ERR_TOO_MANY_REDIRECTS).
# Real pages (services, contact, about, …) are served directly; they must never appear on the left.

# --- Old commercial/litter product slugs (the live ad set) -> the commercial page ---
/shop/p/parking-lot-cleanup        /commercial-cleanup    301
/shop/p/storefront-walkway-cleanup /commercial-cleanup    301
/shop/p/dumpster-area-cleanup      /commercial-cleanup    301
/shop/p/roadside-cleanup           /commercial-cleanup    301
/shop/p/event-cleanup              /commercial-cleanup    301
/shop/p/junk-removal               /junk-removal-rental-estate-cleanout-obx   301

# --- Old store / commerce paths (new site uses Stripe Checkout, no SQSP accounts) ---
/shop        /services    301
/store       /services    301
/account/*   /contact     301
/cart        /services    301
/checkout    /services    301

# --- Google review shortlink — keep working post-cutover ---
/review   https://g.page/r/Cfra8psQX2FLEBM/review   301
