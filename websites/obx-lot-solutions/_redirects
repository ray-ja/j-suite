# OBX Lot Solutions — Squarespace → new site 301 redirects (Cloudflare Pages / Netlify syntax)
# STAGED, NOT DEPLOYED. No DNS cutover without Ray's go.
# ⚠ Left-side OLD paths are ASSUMED from Squarespace conventions — CONFIRM against the
#   Google Ads "Landing pages" Final-URL export before cutover (see websites/301-REDIRECT-MAP.md).
# Targets on the right all exist in this build. Specific rules FIRST, catch-all LAST.

# --- Commercial / litter products (the live Squarespace ad set) → dedicated 1:1 page ---
/shop/p/parking-lot-cleanup        /commercial-cleanup.html    301
/shop/p/storefront-walkway-cleanup /commercial-cleanup.html    301
/shop/p/dumpster-area-cleanup      /commercial-cleanup.html    301
/shop/p/roadside-cleanup           /commercial-cleanup.html    301
/shop/p/event-cleanup              /commercial-cleanup.html    301

# --- Other old products / pages ---
/shop/p/junk-removal   /junk-removal-rental-estate-cleanout-obx.html   301
/shop                  /services.html      301
/store                 /services.html      301
/services              /services.html      301
/about                 /about.html         301
/contact               /contact.html       301

# --- Commerce / account paths (new site uses Stripe Checkout, no SQSP accounts) ---
/account/*   /contact.html    301
/cart        /services.html   301
/checkout    /services.html   301

# --- Google review link — MUST keep working post-cutover. ---
/review   https://g.page/r/Cfra8psQX2FLEBM/review   301

# NO catch-all. A `/*  /  301` here is an infinite redirect loop: the splat matches the homepage
# itself, so `/` 301s to `/` forever (ERR_TOO_MANY_REDIRECTS), and it bounces every new page to
# home. The homepage and all new pages must render directly. Only the specific OLD Squarespace
# slugs above redirect; any unmapped old deep link should fall through to a normal 404, not a loop.
