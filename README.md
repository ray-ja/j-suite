# J-Suite — Jamieson Automation & OBX Lot Solutions

Working repo for two businesses: **Jamieson Automation** (skilled automation / A/V / networking, 100% owned) and **OBX Lot Solutions** (commercial property cleanup + power/window washing). Includes the field app, its sync server, and the growth toolkit.

## Contents

| File | What it is |
|---|---|
| `Business App (v1).html` | Offline-first field app (PWA-style). Runs in any browser on Android + Windows/Linux. Business toggle (separate data per business), customers/notes/status, on-site quote builder with baked-in pricing, today/call list, scheduling. Local storage + optional sync. |
| `sync-server.js` | Zero-dependency Node sync server. Per-record last-write-wins with deletion tombstones, token auth, JSON persistence. |
| `Service Menu & Pricing Plan.md` | Service catalog and pricing for both brands (market-based). |
| `OBX Lot Solutions - Commercial Call List.xlsx` | Prioritized cold-call target list (property managers, HOAs, retail, landscaper partners). |
| `OBX Lot Solutions - Cold Call Playbook.md` | Call scripts + objection handling. |
| `OBX Lot Solutions - Quote Sheet.xlsx` | On-the-spot quote calculator. |
| `OBX Lot Solutions - Flyer (leave-behind).pdf` | Branded leave-behind flyer. |
| `Jamieson Automation - website.html` | One-page marketing site for Jamieson Automation (navy/blue brand, JA logo, two service tiers, request-a-quote). |
| `Jamieson — Product & Install Guide.md` | Quoting reference: smart locks (protocols/brands/install), Starlink, cameras, networking, lighting — what to sell and what's included. |
| `OBX Lot Solutions — Service Guide.md` | Quoting reference: washing/house-watch/junk specs, what's included, sizing, $/hour. |
| `Market Research & Strategy.md`, `Opportunity Analysis — Is It Worth It.md`, `Opportunity & Pricing Model.xlsx` | Overnight research + the income model vs current wages. |
| `Marketing Channel Plan.md` | Every marketing channel ranked by cost & payback (free → big-spend), with cost/eyes/leads per channel, per business. |
| `Brand assets/` | Logos (SVG) for both businesses. |
| `assets/` | App-optimized logos + PWA icons. |
| `manifest.webmanifest`, `sw.js` | PWA manifest + offline service worker (served by sync-server.js). |
| `qb-bridge.js` | QuickBooks Online OAuth + API bridge (zero-dependency). Inactive until `qb-config.json` exists. |
| `qb-config.example.json` | Template for your Intuit keys — copy to `qb-config.json` (gitignored) and fill in. |

## Websites (`websites/` folder)

Two complete, deployable multi-page static sites — no build step, no dependencies:

- `websites/obx-lot-solutions/` — 8 pages (Home, Services, Soft &amp; Pressure Washing, Home Watch, Service Area, Reviews, Contact, Free Estimate). Green/navy brand, trust-signal heavy, click-to-call + sticky mobile call bar.
- `websites/jamieson-automation/` — 8 pages (Home, Services, Starlink, Smart Home &amp; Security, Service Area, Reviews, Contact, Free Consultation). Navy/blue premium/consultative feel.

Each site is now **38 pages** (incl. a **lead-magnet checklist** page with email capture — OBX "Home-Watch Checklist," Jamieson "Rental-Ready Tech Checklist"): the core pages above plus an **About** (founder story), a **FAQ** page (15 Q&As with FAQPage schema) *and* FAQ sections with FAQPage schema on each main service page, a **Resources/blog** hub linking **8 individual keyword-targeted post pages** (each with BlogPosting schema + internal links), a **Project gallery / case studies** page (testimonial structure + photo placeholders), and **16 local-SEO town pages** spanning the northern beaches through **Hatteras Island and Ocracoke** (Corolla, Duck, Southern Shores, Kitty Hawk, KDH, Nags Head, Manteo, Avon, Buxton, Hatteras Village, Rodanthe, Frisco, Ocracoke + service-in-town combos) (per-town + service-in-town, e.g. "Home Watch in Corolla", "Starlink Installation in Corolla"). Both are mobile-first, SEO-ready (unique titles/meta, canonical, Open Graph, LocalBusiness/Service/FAQPage/Blog JSON-LD, `robots.txt`, full `sitemap.xml`), interlinked via a site-wide footer, and have a lead form on the primary CTA that uses **Netlify Forms** (no backend) with fields matching the app's Guided Quote wizard. An automated audit (titles, meta, h1, lang, viewport, dead-link and alt-text checks across all 46 pages and ~1,400 internal links) passes with zero issues. Deploy by dragging each folder onto Netlify (free) — full instructions + the pre-launch checklist are in `websites/DEPLOY.md`, and the app's **💻 Sites** tab summarizes both. Copy in each site reflects the positioning from the Action plans (target customer, pricing posture, what they don't do).

## Running the sync server

```bash
# Linux / macOS:
TOKEN=pick-a-long-secret node sync-server.js       # listens on :4000, stores data.json
```
```powershell
# Windows PowerShell:
$env:TOKEN="pick-a-long-secret"; node sync-server.js
```

In the app's **Data** tab, set the same server URL and token on each device. `data.json` (the live database) is gitignored on purpose — it holds customer data and should never be committed.

**The server also serves the app.** Browse to `http://<server-ip>:4000` from any device on your network (phone, tablet, another PC) to load J-Suite — no file copying. When loaded this way, the sync URL pre-fills to that server automatically; you just enter the token once.

## QuickBooks Online (optional)

The app's invoicing is meant to hand off to QuickBooks, not replace it: the app owns the upstream (leads → quotes → pipeline); QuickBooks owns the downstream (invoices, payments, income). The bridge lets the app push invoices into QBO and pull unpaid/income back.

To turn it on:

1. Create a free account at developer.intuit.com and make an app (enable the **Accounting** scope).
2. Copy `qb-config.example.json` → `qb-config.json` and paste your **Client ID**, **Client Secret**, and redirect URI `http://localhost:4000/qb/callback`.
3. Start the server, then visit `http://localhost:4000/qb/connect` to authorize. Tokens save to `qb-tokens.json` (gitignored).
4. Endpoints: `GET /qb/summary` (unpaid + open invoices), `POST /qb/create-invoice`. (`createInvoice` line mapping gets finalized when we test live against your company file.)

`qb-config.json` and `qb-tokens.json` are gitignored — your keys never leave your machine.

## Brand colors

- **OBX Lot Solutions:** green `#8BC34A` · navy `#1B2A4E` · white
- **Jamieson Automation:** navy `#002052` · blue `#0099E5` · white

## Roadmap

- **Done:** desktop scaling, multi-device sync, month calendar, quote Print/Save-PDF, server-served app + auto-filled sync URL, real logos in-app, installable PWA, invoicing, Today dashboard, customer history, Jamieson Automation website, per-business To-Do page, QuickBooks Online bridge scaffold, dark theme, user accounts + to-do assignment, Plan tab (one-pager/marketing/research), **deep categorized quoting catalogs (grouped dropdown) for both businesses + product/install knowledge guides**, **Map tab: satellite view, draw-a-lot parking-space estimator → one-click quote, tap-to-count mode, saveable lead pins (synced)**
- **Guided Quote wizard** (both businesses): step-by-step flow (customer w/ required name+phone+address → pick services via tiles → per-service calculator with help text, tooltips & sanity warnings → recurring + discount → review → save & present). Powered by a **configurable sliding-scale rate engine** (bigger jobs cost less per unit; rates editable in Settings → Pricing rates). The quick manual builder is still there.
- **Deep line-item estimators — every service, not just Junk.** A shared data-driven engine (`calcDeep`/`wizDeepUI`) gives every quote category the same depth the Junk/move-out tool has: an **itemized line builder** (pick items, set quantities, per-line condition modifiers), **job-condition modifiers with plain-English tooltips** (site access, distance to dump, slope/soft ground, weekend/rush, emergency/after-hours, hazard near wires, repeat-customer discount — each with a real % or $ effect), an **industry-standard "how the price is built" explainer**, a **plain-English glossary**, a **customer-interaction script**, a live **itemized breakdown**, and an honest **± range** where inputs are uncertain (e.g. lot clearing ±15%, storm ±20%). All defaults come from researched 2025-26 benchmarks (see memory: quote-pricing-benchmarks). Covers OBX: house/roof soft wash, concrete pressure, deck, windows, gutters, parking-lot, house-watch, **+ three new land services — Lot/land clearing (per-acre brush + per-tree-by-height + per-inch stump grinding + haul-off), Brush & yard-debris removal (cu-yd / pickup-load), and Storm cleanup (crew + equipment hours + debris haul, with the standard 1.5–2× emergency premium)**. Covers Jamieson: smart locks, PoE cameras, networking/WiFi, Starlink, tech labor. Math verified by isolated unit tests (lot clearing $5,235, storm $8,020, etc. — all match hand-computed values; $0 on empty, $95 floor enforced).
- **In-app deep-rate editor** (Settings → Pricing rates → "Edit deep quote rates"): surfaces *every* number behind the deep estimators — each item rate, each sliding-scale tier, each per-line modifier %, each job-condition % or $, and the minimum charge — grouped per service, with a 📚 **source citation** on each service so the operator knows where the default came from before changing it. Edits save to a per-business overlay (`deepover` doc, merged by `getDeepCfg`) and flow straight into the Guided Quote; per-service and global "reset to researched defaults" buttons included. Override-merge verified by unit tests (rate, tier, per-line mod, job toggle, job-select, and minimum all flow through; reset restores defaults cleanly).
- **Plan tab — Quarterly milestones + "This Week" log.** Two new Plan sub-tabs per business. **Milestones**: quarterly goals (Q2'26→Q1'27) grouped by quarter with per-quarter and overall progress bars, add/edit/check-off, and a one-tap "load starter milestones" seeded from each business's 12-month plan (OBX: launch sites → 5 recurring accounts → trailer + lot clearing → hand brothers a route → $10k/mo; Jamieson: real phone + site → rental-ready installs → PM portfolio → $8k/mo). **This Week**: a glanceable surface showing new quotes (count + $), jobs done, collected, and open pipeline for the last 7 days, plus an **activity log** that auto-captures meaningful events — quote created, invoice paid, job completed, milestone hit — with relative timestamps, grouped This-week / Earlier, plus manual notes. So progress is visible without re-reading anything. Date-window math and log-cap verified by unit tests.
- **Build Plan tab** (🏗️): the meta development plan in three cross-linked sub-tabs. **Architecture** — a rendered SVG diagram of the recommended **5-Claude-instance** setup (Strategy & Research / Chat = the brain; J-Suite Dev / Claude Code = the repo; OBX Field Ops & Jamieson Ops / Cowork; Marketing & Content / Cowork), showing work handoffs, dashed shared-memory links, and the human-approval gate (every send/quote/payment/deploy passes through Rzy) — plus a card per instance (owns / connectors / recurring runs / handoffs). **Brainstorm** — ~60 buildable items across 8 categories (internal apps, customer-facing, calculators, graphics, research, content, automations, training), each with one-line description, owning instance, effort (S/M/L), value (S/M/L), and tier; sortable by tier/value/effort/category and filterable by instance/category/tier; ✓ marks the items already shipped. **Sequence** — a gantt-style 4-lane timeline (This month → This quarter → This year → Later) with per-tier shipped-progress bars, effort breakdown, and "what it unlocks." Every brainstorm item cross-links to its owning instance (Architecture) and its tier (Sequence). Sort/effort/referential logic unit-tested.
- **Build Plan — expand pass.** Brainstorm grown to **99 items** (across all 8 categories, both businesses + Jamieson-specific + speculative aeroponics). Every item now carries a **cost estimate** (build-time priced in working days from its effort, plus a hard-$ note where a tool/subscription is actually required — SMS, email tool, Stripe fees, etc.), shown on each row (⏱) and in the detail modal. New **Built / To-do overlay**: a progress panel at the top of the Brainstorm (built vs to-do counts, % bar, and remaining build-days), a three-way status filter (All / ✓ Built / ◻ To-do), and built items rendered with a ✓ and dimmed. The **Sequence** lanes now roll up per-tier cost as "~X of Y build-days left." Currently **17 of 99 shipped (~82 to go)**. Referential integrity (owners/categories valid across all 99), cost rollups, and filters verified.
- **Build Plan — dependency + leverage layer.** Added a **dependency map** (29 prerequisite links): each item's detail shows what it **Depends on** and what it **Unlocks** as clickable cross-links, with a live **"ready to build now"** flag when every prerequisite is already shipped (and "prerequisites first" when not). Added **leverage** scoring (value ÷ effort): a new **Leverage sort**, a ⚡ **quick-win** marker on high-leverage rows, and the leverage figure in each detail. All 15 prerequisite ids verified to resolve to real items, no backward-tier dependencies, and ready/leverage logic unit-tested. Also shipped: the **Claude Instance Setup Packet** (paste-in instructions for all 5 lanes + Claude Code install steps + connector lists + handoff protocol).
- **Accounts restructure**: "Accounts" tab with **Customers** + **Properties** sub-tabs. Properties are their own records (own address + access notes + lat/lng), **many-to-many** with customers (owner + property manager on one property; many properties per customer). "How they found us" lives on the customer; access notes on the property. Address fields have **autocomplete** (OpenStreetMap). Quotes/jobs link to a customer + a property. Existing data auto-migrates.
- **Sales tab — two route planners** (free OSRM optimization): a **prospecting route** (check target-account pins + add addresses → optimized loop) and a **job route** (a day's jobs with mapped properties → optimized drive). Both draw the route on satellite, show miles + drive time, and have a **Navigate** button that hands off to Google Maps for live turn-by-turn. Jobs can now link to a property for mapping.
- **CEO desk** (home tab): a standing directive from the "CEO" — today's one move, this week, and the anti-overwhelm rule — plus a live snapshot (pipeline / unpaid / jobs this week). Editable per business. Designed to fight planning-paralysis: look here, do the top thing.
- **Market tab** (📊): deep market research surfaced in-app, browsable as sub-reports per business (OBX Lot Solutions · Jamieson Automation · Aeroponics) plus a cross-business **Expansion** view. Within each business's Research view, a **per-industry tab strip** lets you drill into one industry at a time — OBX splits into Pressure & soft washing · Junk removal · Home watch · Commercial cleanup; Jamieson into Automation/control · Security & cameras · Networking & wifi · Starlink · AV & lighting; Aeroponics into Consumer hardware · B2B microgreens · Large-scale — each with its own size KPIs, a focused chart, and that industry's specific players, alongside the combined **★ Overview**. Each report is sectioned in plain English — Market Size (with KPI tiles + a share/trajectory chart) · The Players · Local Competitors (real named OBX businesses with sites/services/pricing/size) · Comparable Companies (out-of-area operators at the size to grow into) · How Money Comes In (every revenue stream with ticket/frequency/LTV, flagging streams not yet offered) · How It Grows (growth levers with investment/horizon/failure mode + real case studies) · Revenue Examples (small/mid/upper real-size operators). In-app SVG donut + bar charts, cited sources, and honest "this is an estimate" labels where no public data exists (esp. OBX-local share and consumer aeroponics).
- **Opportunities tab** (💡 Opps): an 80-idea OBX income scan weighted toward low-maintenance, AI-automatable, passive-leaning plays. Each opportunity carries scannable chips (maintenance hrs/wk, setup $, monthly revenue range, AI-leverage 1-5, time-to-first-revenue) and a tap-to-expand detail with what it is, why OBX specifically, realistic low/mid/upside revenue, the biggest risk, and the first 3 Monday-morning actions. Six views: **⚡ Passive shortlist** (top 10 ranked by revenue ÷ maintenance hours, led by a revenue-vs-maintenance scatter where AI-leverage is the dot color/size), **🚀 Quick wins** (launchable within ~30 days), **🗓 Launch plans** (week-by-week first-30-days for the top 5 passive picks), **✅ Proof** (real named national operators with revenue + an OBX-local-proof group of businesses already running these here + honest reality-checks), **▦ Table** (sortable desktop grid of all 80 with a Ray-vs-brothers team column), and **📂 All** with live sort (leverage / revenue / setup / maintenance / AI / speed), category filters across ten categories (AI tools, automation services, content/creator, web/digital, digital products, light services, tourist micro-services, marketplaces, passive assets, big plays), and a **team-fit filter** (Anyone / Owner-only / hand-off-able to Pierce & Chase). 36 ideas rate AI-leverage 4-5; 15+ need under 2 hrs/week and under $1,000 to start.
- **Organization & graphics layer** (across Market / Plan / Opportunities): each tab opens with a **TL;DR strip** (3 headline tiles + "top recommendation" / "riskiest assumption" callouts). Long research prose stays intact but lives in collapsible sections; key points are pulled into colored **callout boxes** (insight / warning / info). Competitors render both as full profiles and as **side-by-side comparison tables**; opportunities carry **status pills** (Quick win / Build-up / Long-heavy) on their scan cards plus a category-breakdown bar. Plans show a **30/60/90 timeline strip**. **Cross-link buttons** connect the Market research and Action plan views so the three tabs read as one system. Charts use inline SVG (donuts, bars, scatter, columns) — no new dependencies.
- **Action plans** (inside 📊 Market — toggle **🎯 Action plan** vs **📊 Research** per business): a complete, decision-ready 12-month plan for OBX Lot Solutions, Jamieson Automation, and Aeroponics, built entirely on the market research. Ten sections each — (1) honest baseline, (2) 12-month target with a now-vs-target bar, (3) the specific target customer, (4) ranked channel plan (spend / expected return / how to measure / when to cut) with a lead-potential chart, (5) the concrete pricing change to make and the revenue lift, (6) 1-2 revenue streams to add, (7) the ONE growth lever with investment/horizon/first-3-milestones, (8) a 30/60/90-day checklist with an owner + cost per action and a **one-tap "Load 30-day actions into my To-Do list"** button that writes the items straight into the right business's To-Do, (9) what we're explicitly NOT doing, (10) a 12-month monthly-revenue projection (column chart + revenue/cost/net tiles). Each plan ends with an honest caveat where the data was thin.
- **Training tab** (both businesses): a built-in academy. Six in-depth modules each (know your products/services, getting clients, sales scripts for non-salespeople, quoting, doing the job, after-care/reviews/recurring), each with a multiple-choice **quiz** and a **sign-off** recorded per signed-in user (you + each brother track their own; progress bar shows % complete). Heavy on word-for-word scripts and objection handling. The deeper field references stay in the Service Guide + Product & Install Guide.
- Next: get Intuit keys → QuickBooks live; online booking + payments; HTTPS for phone PWA; friendlier rates editor; self-host OSRM if prospecting volume grows

### Notes on accounts
Accounts are lightweight identity for to-do assignment, **not** hardened security — data lives in your local/synced store. Passwords are hashed (SHA-256) but a determined person with device access can read the data. Fine for a trusted 2–3 person crew; don't treat it as a security wall. Users sync across devices; the signed-in user is per-device.
